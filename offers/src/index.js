import React from 'react';
import ReactDOM from 'react-dom/client';
import OfferProvider from './offerpg';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <OfferProvider />
  </React.StrictMode>
);


